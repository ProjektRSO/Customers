query allCustomers {
   allCustomers(pagination: {offset: 0, limit: 10},
    						sort: {fields: [{field: "id", order: ASC}]}) {
    result {
    	id
    	firstName
    	lastName
    }
    pagination {
      offset
      limit
      total
    }
  }
}

query getCustomers {
	Customers(id:1){
        id
        firstName
        lastName
        streetAddress
  }
}

mutation addCustomers {
  addCustomer(imageMetadata: {title: "Šesta slika", description: "Uploaded via GraphQL"}){
    id
    firstName
    lastName
  }
}

mutation deleteCustomer{
  deleteCustomer(id: 1){
    deleted
  }
}